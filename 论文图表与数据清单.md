# 论文图表、数据与补实验总清单

> 项目：`My_sdp_test`  
> 对应草稿：`D:/组会/2026/8.8/新建 DOCX 文档.docx`  
> 审阅基线：Git commit `3063477`  
> 盘点日期：2026-08-12  
> 论文暂定题目：面向异构 WiFi CSI 感知数据集的可插拔预处理管线的系统评测与分析

## 1. 先给结论

这篇论文最有价值的主线不是“哪一条管线准确率最高”，而是：

1. 同一套可插拔预处理组件在异构 CSI 数据、任务和模型上会产生很强的差异；
2. 这种差异不仅来自单一算法的优劣，还来自数据长度、采样率、复数/实数语义、执行次序以及组件交互；
3. 项目已经积累了足以支撑这条主线的大量结果，尤其是 Widar、XRF55、ElderAL 的细粒度组合实验，以及 Hampel、带通滤波、Robust 相位校准三个根因案例；
4. 目前不能直接定稿的主要原因是 Gait 细粒度实验缺 67/320 组、主体结果几乎都是单随机种子、主汇总只有 Accuracy、资源开销未按统一口径测量，部分模型存在 OOM/跳过但草稿尚未披露；
5. 推荐将正文控制为 8 幅组合图和 9 张核心表，其余完整矩阵、训练曲线、失败日志和诊断波形放附录/补充材料。

草稿目前共 15 页，已有 5 张表、0 幅图；摘要为空，参考文献均为占位符，结果章节仍有大量红色 `XX`，Robust 相位校准、总结与结论也尚未完成。因此，当前最优先的工作不是继续堆文字，而是先锁定数据口径、补齐关键实验，再按本文清单统一出图。

## 2. 论文建议围绕四个研究问题组织

| 研究问题 | 回答什么 | 主要证据 |
|---|---|---|
| RQ1：预设管线是否可跨数据集、跨模型稳定工作？ | 6 个 preset 在 4 个数据集、19 个模型上的性能、覆盖率与失败模式 | 第一阶段 4×114 条任务记录；模型×preset 热力图；成功/失败/跳过统计 |
| RQ2：单个预处理组件及其交互怎样影响结果？ | 去噪、异常值、相位校准、归一化和插值的边际效应与交互 | Widar/Gait 320 组合；XRF55/ElderAL 80 组合；匹配配对差值 |
| RQ3：为什么某些“合理”算法会在特定数据上失败？ | 短序列 Hampel 过度替换、带通输出符号被绝对值破坏、Robust 斜率外推失真 | 三个根因消融及波形/统计诊断 |
| RQ4：如何据数据属性选择预处理管线？ | 从“算法排名”上升到可执行的选择规则 | 数据属性—算法适用性矩阵、证据强度和决策流程图 |

这四个问题能将目前较分散的“全面实验 + 三个异常案例”收束为一条完整叙事：**广泛测量 → 识别敏感性 → 解释机制 → 给出指南**。

## 3. 证据状态与使用规则

本文后续所有图表建议使用以下标记：

| 标记 | 含义 | 是否可进入定稿 |
|---|---|---|
| A：可直接生成 | 原始汇总完整，口径基本明确 | 可以，但仍需统一绘图风格和核对标签 |
| B：可从现有产物重算 | 有 checkpoint、日志、JSON、波形或预测产物，但还需聚合/配对/重绘 | 重算并审计后可以 |
| C：必须补跑 | 数据缺失、实验不完整、只有单 seed 或指标未保存 | 不应作为强结论，完成后再定稿 |
| P：暂时性证据 | 可用于组会或论文工作稿，但图中必须标 `Preliminary` | 不可在定稿中伪装为完整结果 |

统计上的重要原则：320/80 个管线组合是“实验条件”，不是独立随机重复，不能把组合间方差当作 seed 方差。组合敏感性可以使用匹配配对差值和 bootstrap 描述，但训练随机性的置信区间必须来自多随机种子。

## 4. 当前数据完成度快照

### 4.1 第一阶段：6 个 preset × 19 个模型

每个数据集名义上有 114 个任务。`ok` 表示有有效测试准确率；`failed` 与 `skipped` 必须作为覆盖率公开，不能将空项当作 0，也不能声称所有 19 个模型均完成公平比较。

| 数据集 | 总任务 | ok | failed | skipped | 有效覆盖率 | 所有有效结果 Accuracy 范围 | 中位数 |
|---|---:|---:|---:|---:|---:|---:|---:|
| Widar | 114 | 84 | 13 | 17 | 73.68% | 52.93%–68.69% | 58.89% |
| Gait | 114 | 84 | 12 | 18 | 73.68% | 41.56%–92.89% | 78.58% |
| XRF55 | 114 | 84 | 0 | 30 | 73.68% | 14.70%–84.70% | 79.85% |
| ElderAL | 114 | 106 | 8 | 0 | 92.98% | 16.67%–91.48% | 64.81% |

Widar/Gait 当前完整成功的模型均为 14 个：`attentiongru`、`bilstmattention`、`cnn1dmodel`、`csimodel`、`csitime`、`ei`、`fewsense`、`lstmmodel`、`mlpmodel`、`pa_csi`、`resnet1d`、`resnet2d`、`that`、`wiflexformer`。未完成项主要涉及配置跳过以及 CNN2D、EfficientNet、ViT 等的显存不足；具体原因必须进入附录失败表。

第一阶段代表模型在 6 个 preset 下的范围如下，可用于验证草稿中的代表模型选择：

| 数据集 | 代表模型 | 最低 preset / Accuracy | 最高 preset / Accuracy |
|---|---|---|---|
| Widar | MLP | localization / 62.83% | gesture_recognition / 68.69% |
| Gait | MLP | localization / 78.66% | high_quality / 91.73% |
| XRF55 | ResNet1D | fast / 81.82% | localization / 84.09% |
| ElderAL | CSITime | fast / 82.59% | high_quality / 91.48% |

### 4.2 第二阶段：细粒度管线组合

复数数据 Widar/Gait 的设计空间为 `5 去噪 × 2 异常值 × 4 相位校准 × 2 归一化 × 4 插值 = 320`。实数表征 XRF55/ElderAL 不使用相位校准，设计空间为 `5 × 2 × 2 × 4 = 80`。

| 数据集 | 模型 | 参数量 | 预期/已完成 | 完成度 | Accuracy 最低/中位/均值/最高 | 状态 |
|---|---|---:|---:|---:|---|---|
| Widar | MLP | 663,619 | 320/320 | 100% | 51.92 / 68.69 / 67.10 / 76.67% | A |
| Gait | MLP | 665,161 | 253/320 | 79.06% | 26.47 / 91.12 / 81.05 / 96.48% | **P/C** |
| XRF55 | ResNet1D | 571,959 | 80/80 | 100% | 36.67 / 83.18 / 74.75 / 85.15% | A |
| ElderAL | CSITime | 119,558 | 80/80 | 100% | 58.52 / 82.59 / 79.53 / 92.59% | A |

**Gait 特别说明：**缺失的是连续编号 254–320，共 67 组。按当前枚举次序，这不是随机缺失，而集中在设计空间尾部，包含剩余 Robust/min-max 以及 Hampel 相关组合。因此，当前 Gait 的均值、中位数、极值、组件边际效应和排名均有选择偏差，只能标为暂时结果；补齐前不得把 96.48% 写成最终“最优管线”，也不能做跨数据集完整排名。

当前完整数据中可核验的极值组合：

| 数据集 | 当前最佳组合 | Accuracy | 当前最差组合 | Accuracy |
|---|---|---:|---|---:|
| Widar | Savitzky–Golay + z-score 异常检测 + linear 相位校准 + z-score 归一化 + nearest 插值 | 76.67% | Wavelet + IQR + Robust 相位校准 + min-max + decimate | 51.92% |
| Gait（暂时） | Savitzky–Golay + IQR + linear 相位校准 + z-score + decimate | 96.48% | Savitzky–Golay + IQR + Robust 相位校准 + min-max + decimate | 26.47% |
| XRF55 | Savitzky–Golay + IQR + z-score + cubic | 85.15% | Bandpass + IQR + z-score + nearest | 36.67% |
| ElderAL | Savitzky–Golay + IQR + min-max + linear/decimate（并列） | 92.59% | Hampel + IQR + z-score + cubic | 58.52% |

这些极值适合用于引出根因问题，但不能仅凭一个 seed 宣称算法普遍优劣。

### 4.3 数据集、输入与实际划分

草稿中统一写成 70/15/15 并不准确。分组划分会产生离散比例，论文应优先给出实际样本数和 group 定义，而不是只给目标比例。

| 数据集 | 任务 | 总样本/类别 | 模型输入 | Train/Val/Test | 实际比例 | 划分依据 |
|---|---|---|---|---|---|---|
| Widar | 手势分类 | 6,750 / 3 | `(1500, 15, 6)` | 4,725 / 1,035 / 990 | 70.00 / 15.33 / 14.67% | condition 分组 |
| Gait | 用户识别 | 18,657 / 9 | `(1500, 15, 6)` | 12,430 / 3,106 / 3,121 | 66.62 / 16.65 / 16.73% | user_id_v2 对应 group 分组 |
| XRF55 | 动作识别 | 3,300 / 55 | `(1000, 15, 9)` | 1,980 / 660 / 660 | 60 / 20 / 20% | repetition 1–12 / 13–16 / 17–20；当前取 3 个用户 |
| ElderAL | 动作识别 | 2,404 / 6 | `(80, 64, 3)` | 1,600 / 534 / 270 | 66.56 / 22.21 / 11.23% | position/group 分组 |

论文还应导出并附上每个 split 的 group ID、类别计数和样本索引哈希，以消除“同一比例但样本不同”的复现歧义。

## 5. 正文建议图：8 幅组合图

### 图 1：研究总览与贡献图（P0，需新画）

- **目的：**一图说明异构 CSI 输入、统一可插拔协议、两阶段评测、根因诊断和最终指南。
- **布局：**左侧 4 个数据集属性卡片；中间统一管线 `去噪 → 异常值 → 相位校准（仅复数）→ 归一化/插值`；上支路为 6 preset × 19 models；下支路为 320/80 组合；右侧输出性能敏感性、三个失败机制和选择指南。
- **必须标明：**复数与实数分支、数据集专属 target_K、分组划分、缓存位置不进入模型拟合。
- **数据需求：**无新增实验；来自代码和配置审计。
- **借鉴：**仓库内 SDP 论文 Fig. 1–2 的“动机 + 协议”表达，但不要照搬视觉设计。

### 图 2：第一阶段模型 × preset 全景热力图（P0，A）

- **布局：**四个数据集各一个面板，横轴 6 presets，纵轴 19 models，颜色为测试 Accuracy。
- **缺失显示：**`failed` 用红色斜线，`skipped` 用灰色，绝不能填 0；每个面板标题写 `ok/114`。
- **排序：**模型按模型家族或跨 preset 中位 Accuracy 排序；所有面板保持同一顺序。
- **附加元素：**右侧一列显示每个模型的 preset 极差；底部显示每个 preset 的有效模型数。
- **要回答：**预设的稳定性、模型对预处理的敏感程度、覆盖率差异。
- **数据源：**四个 `*_all_presets_models_summary.csv`。

### 图 3：第二阶段组件效应与组合分布（P0；Widar/XRF55/ElderAL=A，Gait=C）

- **不要画：**四张挤满 320 根柱子的图。
- **推荐布局：**每个数据集一个面板，按组件层级绘制 raincloud/box/strip；纵轴 Accuracy；点表示管线条件。
- **上半部分：**去噪方法分组分布；下半部分：归一化、插值、相位校准的匹配配对差值。
- **关键标记：**每组写有效 `n`，Gait 在补齐前加斜纹和 `79.06% complete` 水印。
- **统计：**主图使用匹配条件下的平均百分点差；误差条使用对匹配组合 bootstrap 的描述性 95% CI；正文明确其不是跨 seed CI。
- **要回答：**哪些组件效应大，哪些只是轻微扰动，数据集间方向是否一致。

### 图 4：关键交互效应图（P0；需重算，B/C）

- **建议选择三组最有论文价值的交互：**
  1. 相位校准 × 归一化（Widar/Gait）；
  2. 去噪 × 序列长度/采样率（四数据集）；
  3. 去噪 × 插值（XRF55/ElderAL）。
- **绘图：**使用 matched slope / interaction plot 或 effect-size forest plot，不使用仅按组均值的误导性柱图。
- **已知强信号：**Robust 相对 linear 在 Widar 的 80 个匹配对上平均约 −10.49 个百分点；Gait 当前 48 个匹配对上约 −47.80 个百分点。Gait 数值因实验不完整只能标暂时结果。
- **要回答：**性能不是单组件可加和，执行次序和组合会放大损伤。

### 图 5：三类失败机制组合图（P0，部分 A/B/C）

建议做成 3 行 × 3 列的核心机制图，每行一个案例，统一遵循“输入条件 → 信号变化 → 任务后果”。

1. **Hampel × ElderAL 短序列**：原始/处理后代表波形、替换比例或保留率、窗口长度—Accuracy 曲线；现有 seed 42 下无 Hampel 90.74%，窗口 3/5/7/11 帧分别约 78.15/70.37/62.96/68.52%。进入定稿前应做 5 seeds。
2. **Bandpass × XRF55 符号语义**：负值占比、`abs()` 前后趋势方向变化、signed/absolute 分类对照；现有信号分析显示带通输出约一半为负值，当前仅有 seed 42 的 fs=200 对照（signed 64.70%，legacy abs 62.58%），计划 7 条件 × 5 seeds 中尚缺 33/35。
3. **Robust × 长序列相位外推**：前 50 帧拟合示意、全序列相位斜率偏移/一致性、linear 与 Robust 的配对 Accuracy；现有机制统计中，首 50 帧与全时段斜率相关仅约 0.299（Gait）和 0.371（Widar），需要统一重绘。

当前 `bandpass_sampling_sign_ablation.png` 带有 `INCOMPLETE` 水印，只能用于组会，不能直接进入终稿。

### 图 6：多随机种子稳定性与统计不确定性（P0，C）

- **推荐对象：**每个数据集选择 baseline、当前最佳、当前中位、当前最差，以及根因对照条件；每个条件至少 5 seeds。
- **绘图：**seed 点 + 均值 + 95% CI；另给 rank stability（各 seed 排名）或 paired delta。
- **不用全补：**无需把所有 1,040 个细粒度组合都跑 5 seeds。应优先覆盖论文强结论与极端组合。
- **要回答：**观察到的差异是否超过训练随机性，推荐规则是否稳定。
- **借鉴：**仓库内 SDP 论文 Fig. 3–6 的 CI、稳定性分布与排名一致性组织方式。

### 图 7：错误结构与类别级指标（P1，B/C）

- **布局：**每个数据集选择最终推荐管线，绘制归一化混淆矩阵；旁边给 per-class F1。
- **对照：**根因案例可增加 baseline vs failure pipeline 的混淆矩阵差分图，而不是列出所有组合。
- **指标：**Accuracy、macro-F1、balanced accuracy、每类 precision/recall/F1；类别不平衡时主文不能只报 Accuracy。
- **现状：**主汇总 CSV 只有 Accuracy。若现有 `test_predictions.npz` 覆盖不足，需要从 checkpoint 重新推理并统一导出 `y_true/y_pred/logits/group_id`。

### 图 8：预处理选择指南/决策图（P0，定稿时生成）

- **输入属性：**复数/实数表征、序列长度、采样率可信度、是否保留符号、是否需要相位、目标子载波数、异常脉冲密度。
- **输出：**推荐、谨慎、避免三档，并附证据强度（多数据集多 seed / 单数据集多 seed / 机制证据 / 暂时观察）。
- **示例规则：**短序列不要默认使用大窗口 Hampel；带通后不要无条件取绝对值；长序列相位不要用短前缀斜率全程外推；插值和归一化次序必须固定并报告。
- **性质：**这是全文贡献总结，不是主观经验表；只能在关键补实验完成后冻结。

## 6. 正文建议表：9 张核心表

### 表 1：相关基准与本文定位（P1）

列建议：工作、数据集数、任务数、模型数、是否统一协议、是否系统比较预处理、是否分析组件交互、是否做根因诊断、是否报告资源/复现信息。

对比对象至少包含：SenseFi、SDP、Widar3.0、SignFi 以及与四个数据集直接相关的原始论文。本文的差异化应落在“异构数据上的可插拔预处理系统评测和失效机制”，不要声称创建新的通用模型基准。

### 表 2：数据集、任务和严格划分（P0）

使用第 4.3 节的实际样本数，同时补充：原始数据类型、模型输入表征、采样率、序列长度、天线/子载波维度、类别数、group 定义、split ID、是否存在类别不平衡。明确 XRF55 是全数据还是 3-user 子集。

### 表 3：可插拔组件、参数与执行次序（P0）

列建议：阶段、候选算法、参数、适用数据类型、fit 范围、transform 范围、输出语义。必须单独写清实际执行次序：当前 z-score 路径与 min-max 路径的复数处理位置并不完全相同，这会产生交互/混杂，不能只给一个抽象箭头。

### 表 4：六个 preset 的完整定义（P0）

草稿已有雏形，应补：所有参数值、目标子载波数、复数/实数适用规则、与代码配置的精确映射、配置哈希。不要只写算法名。

### 表 5：模型与任务覆盖表（P1）

列建议：模型、家族、输入要求、参数量、4 数据集各自 `ok/failed/skipped`、失败原因。OOM 是部署/可扩展性证据，不能隐去；若决定在更小 batch 重跑，应同时报告原公平设置和兼容设置。

### 表 6：训练协议与复现环境（P0）

草稿已有超参数表，应再加入：随机种子列表、早停、模型选择指标、每个模型 batch size、optimizer/scheduler、最大 epoch、AMP、单 GPU/多 GPU实际用法、CUDA/PyTorch/NumPy/SciPy 版本、GPU 型号、commit、数据清单哈希。不能只写“2×A100 80 GB”而不说明单任务是否真正用了两张卡。

### 表 7：两阶段主结果摘要（P0）

第一部分报每数据集第一阶段覆盖率、代表模型和 preset 区间；第二部分报细粒度组合完成度、median、best、worst、range。Gait 行必须在 320/320 后才能转为最终表。

### 表 8：组件主效应与交互效应（P0，B/C）

每行一个有预注册/明确对照的比较；列为数据集、对照、匹配对数、平均 Accuracy 差（百分点）、median 差、bootstrap CI、seed 数、证据等级。把“配置层面的描述性不确定性”和“训练 seed 不确定性”分开。

### 表 9：根因证据与最终建议（P0，C 后冻结）

每行一个问题：触发条件、观测到的信号破坏、任务级影响、多 seed 结果、建议修复、适用边界。三行分别为 Hampel、Bandpass、Robust；这张表可成为结论章节的核心。

## 7. 附录/补充材料应保留的图表和数据

1. 四个数据集完整模型 × preset 数值矩阵；
2. 320/80 个组合的完整配置、Accuracy、状态、配置哈希；
3. 所有 failed/skipped 任务、异常类型、显存和重试策略；
4. 最终管线及根因对照的完整混淆矩阵、per-class 指标；
5. 选中条件的训练/验证曲线与早停 epoch；
6. Hampel 的窗口跨度、替换 run、信号保留率和更多类别波形；
7. Bandpass 计划中的 7 条件 × 5 seeds 全结果，采样率 200/1000 的频率响应；
8. Robust 相位拟合诊断、首 50 帧/全时段斜率统计、组件拆分消融；
9. 预处理 CPU 时间、峰值 RAM，模型 FLOPs、参数量、推理延迟和峰值 GPU 显存；
10. 数据 manifest、split 文件、环境 lock、配置文件和生成所有图表的脚本入口。

## 8. 必须补齐或重算的数据

### 8.1 P0：投稿前阻塞项

1. **补齐 Gait 67 组。**完成后重新生成整个 Gait 汇总，验证无重复 index、无空配置、所有 320 个配置哈希唯一，再重算所有最佳/最差/边际/交互结果。
2. **完成 Bandpass 官方消融。**当前 35 个计划运行仅 2 个完成；需要补 33 个，并在同一数据、同一 split、同一模型和同一 seed 集下比较采样率、signed/abs 语义及 baseline。
3. **为强结论增加多 seed。**建议每数据集选 baseline、best、median、worst 和关键根因对照，各 5 seeds；Hampel 窗口、Robust 组件拆分也至少 5 seeds。
4. **统一导出类别指标。**保存预测以计算 macro-F1、balanced accuracy、per-class precision/recall/F1 和混淆矩阵。
5. **冻结 split 和配置。**输出 split IDs、样本/类别计数、配置 JSON 和哈希；论文中的所有数值必须能追溯到唯一 run ID。
6. **核对执行语义。**尤其是复数归一化、`abs/angle` 提取与插值的先后关系，以及 XRF55 中“原始复数数据”和“模型使用的实数/有符号表征”的准确措辞。

### 8.2 P1：显著增强可信度

1. **资源微基准。**当前汇总中的 `duration_sec` 混合了预处理、缓存命中和训练，不能直接横向比较。应在固定样本数、清空/明确缓存状态下测每个预处理组件 CPU 时间和峰值 RAM；模型部分测参数量、FLOPs/MACs、单样本或固定 batch 延迟、峰值显存，报告 median/IQR。
2. **失败模型处理策略。**二选一并提前写入协议：保持固定资源预算，把 OOM 作为兼容性结果；或用预定义 batch 降级规则重跑，并同时披露原始失败。不要在看到结果后随意调整。
3. **实际 group 划分审计。**确认训练、验证、测试 group 无重叠；输出每类在三个 split 的计数图或表。
4. **模型参数量全表。**日志已有大量 `模型参数量`，可自动解析；FLOPs 与推理延迟仍需统一计算。

### 8.3 P2：篇幅允许时

1. 不同模型家族上复验第二阶段精选管线，验证“预处理结论是否依赖代表模型”；
2. 对推荐管线做跨环境/跨域或少样本评估；
3. 预处理组件对校准误差（ECE）、鲁棒噪声和缺包的影响；
4. 训练前数据质量指标与最终性能的相关性，作为低成本管线筛选器。

## 9. 三个根因案例的现有证据边界

### 9.1 Hampel

- ElderAL 中现有波形、窗口跨度、替换行为和 seed 42 任务 Accuracy 已能形成完整机制假设；
- 短序列只有 80 帧，大窗口会覆盖显著比例的时序结构，这是非常适合正文的物理解释；
- 目前分类曲线主要是单 seed，终稿需多 seed；
- XRF55 的窗口 3/5/11/21/51/101 帧已有单 seed 结果，Accuracy 约为 80.91/82.88/83.18/84.55/84.39/85.15%，可用于说明窗口效应具有数据依赖性，而不是“Hampel 总是有害”。

### 9.2 Bandpass

- ElderAL 因序列过短而触发 bypass 的比例为 83.61%，且 bypass 输出与输入完全一致，说明很多配置名义上使用了 Bandpass、实际却没有处理；这应进入有效管线定义和审计表；
- XRF55 信号分析已显示 Bandpass 输出约一半为负值，后续取绝对值会改变方向与能量语义；
- 当前分类对照只有 2/35，且最终报告本身标记为 `PRELIMINARY`，因此只能写成假设与初步证据，不能下因果结论。

### 9.3 Robust 相位校准

- 代码按每个子载波用前 50 帧估计斜率，再外推到 1500 帧；外推跨度约为拟合跨度的 30.59 倍；
- 首 50 帧与全时段斜率的低相关以及相邻窗口漂移支持“局部斜率不代表全局”的机制；
- Widar 的 matched Robust–linear 差值已有 80 对，平均约 −10.49 个百分点；
- Gait 当前差值很大，但仅覆盖部分组合，组件消融也未形成完整多 seed 设计，必须等待补齐后再冻结数值。

## 10. 参考优秀论文时具体借鉴什么

### 10.1 SDP: A Unified Protocol and Benchmarking Framework for Reproducible Wireless Sensing

仓库已包含该论文 PDF。最值得借鉴的是：

- Fig. 1–2 用“问题—协议—评测”快速建立研究结构；
- Fig. 3 用均值/95% CI 和归一化混淆矩阵共同报告总体性能与错误结构；
- Fig. 4–6 用五随机种子、稳定性分布、消融和排名一致性避免只报单次最优；
- 表格同时覆盖数据集、协议和算法无关比较，而不是只列 Accuracy。

### 10.2 SenseFi: A Library and Benchmark on Deep-Learning-Empowered WiFi Human Sensing

可借鉴其基准维度：任务与平台、准确率、模型大小、计算复杂度、特征迁移和无监督设置。本文不必复制全部任务，但至少要补齐参数量、计算/延迟和跨模型覆盖率。[论文页面](https://pmc.ncbi.nlm.nih.gov/articles/PMC10028433/)

### 10.3 Widar3.0

可借鉴其跨位置、朝向和环境等 domain 维度的实验叙事。本文即使不新增完整跨域实验，也应在局限性中明确：当前结论来自特定 split 和数据子集，不能自动外推为跨域鲁棒性。[官方项目页](https://tns.thss.tsinghua.edu.cn/widar3.0/index.html)

### 10.4 SignFi

可借鉴其对数据采集设置、任务定义和类别规模的清晰描述。本文的四个数据集也应统一使用一张“采集—输入—任务—划分”表，而不是分散在段落中。[官方项目页](https://yongsen.github.io/SignFi/)

## 11. 已安装的论文写作技能

已选择并安装原生 Codex 版本的 `academic-research-suite`：

- 项目：[`Imbad0202/academic-research-skills-codex`](https://github.com/Imbad0202/academic-research-skills-codex)
- 安装位置：`C:/Users/21669/.codex/skills/academic-research-suite`
- 当前适配版本：0.1.24
- 选择理由：它把文献调研、学术写作、引用完整性、图表与正文一致性、实验 provenance、缺失证据标注和审稿检查放在同一工作流中；比单纯的“润色 prompt”更适合本项目。
- 使用说明：技能列表通常在新任务/下一轮会话加载；本次盘点没有假装调用尚未热加载的技能。后续正式改写摘要、实验结果和 Related Work 时可以直接指定它。

## 12. 推荐的实际执行顺序

1. 补齐 Gait 67 组，并完成完整性审计；
2. 完成 Bandpass 33 个缺失运行；
3. 冻结数据 manifest、split、配置哈希和指标导出格式；
4. 从完整单 seed 组合中选定需要 5 seeds 的少量代表条件并补跑；
5. 重算 matched effects、交互、macro-F1、balanced accuracy 和混淆矩阵；
6. 做统一资源微基准；
7. 先生成图 2–7 和表 2–9，再据结果写摘要、结果章节、Robust 小节和结论；
8. 最后画图 1 和图 8，因为它们分别概括最终方法结构和最终经验规则；
9. 全文执行一次“图表数值—正文陈述—配置来源”三向一致性审计。

## 13. 关键结果文件索引

### 第一阶段

- `SDP/test_wider/result/preset_tests/condition_v2_all_presets_models_summary.csv`
- `SDP/test_gait/result/preset_tests/user_id_v2_all_presets_models_summary.csv`
- `SDP/test_xrf55/result/preset_tests/train_norm_nosplit_repetition_all_presets_models_summary.csv`
- `SDP/test_elderAL/result/preset_tests/source_action_position_all_presets_models_summary.csv`

### 第二阶段

- `SDP/test_wider/result/full_tests_new/widar_320_pipeline_optimized_mlpmodel_summary.csv`
- `SDP/test_gait/result/full_tests_new/gait_320_pipeline_optimized_mlpmodel_summary.csv`
- `SDP/test_xrf55/result/full_tests/xrf55_80_pipeline_resnet1d_summary.csv`
- `SDP/test_elderAL/result/full_tests/elderAL_80_pipeline_csitime_summary.csv`

### 根因消融

- `ablation/ablation_elder_hampel_result/`
- `ablation/ablation_xrf55_hampel_result/`
- `ablation/bandpass_server_results/final_report/bandpass_final_report.md`
- `ablation/bandpass_server_results/signal_analysis/`
- `ablation/ROBUST_ROOT_CAUSE_REPORT.md`
- `result/ablations/robust_components/gait/summary.csv`
- `ablation/robust_component_results/gait/summary.csv`

## 14. 最终提交前的“红线”检查

- 不把 failed/skipped 写成 0，也不悄悄从分母删除；
- 不在 Gait 只有 253/320 时给最终排名和完整组件效应；
- 不把单 seed 差异包装成带统计显著性的普遍结论；
- 不把 320 个配置当 320 次随机重复；
- 不直接横向比较受缓存影响的 `duration_sec`；
- 不使用带 `INCOMPLETE` 水印的 Bandpass 图进入终稿；
- 不只写目标 split 比例，应写实际样本数和 group；
- 不用“实数/复数”模糊掩盖 `abs()`、signed value、amplitude/phase 提取的真实执行语义；
- 不在图中只展示最佳结果，必须同时给覆盖率、失败、分布与不确定性；
- 每个正文结论都能追溯到 run ID、配置、commit、split 和原始指标文件。

